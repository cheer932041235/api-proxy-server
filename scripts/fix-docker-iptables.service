[Unit]
Description=Fix Docker iptables FORWARD rules
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/fix-docker-iptables.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
